<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Crystal Lotus - Tienda Oficial</title>
    
    <!-- ESTILOS (Tailwind) -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- FUENTES -->
    <link href="https://fonts.googleapis.com/css2?family=Fredoka:wght@300;400;500;600;700&family=Quicksand:wght@400;500;600;700&display=swap" rel="stylesheet">
    
    <!-- REACT Y BABEL (Versiones Estables) -->
    <script crossorigin src="https://unpkg.com/react@18/umd/react.production.min.js"></script>
    <script crossorigin src="https://unpkg.com/react-dom@18/umd/react-dom.production.min.js"></script>
    <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
    
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: { sans: ['Quicksand', 'sans-serif'], display: ['Fredoka', 'sans-serif'] },
                    colors: { brand: { 50: '#fdf2f8', 100: '#fce7f3', 500: '#ec4899', 600: '#db2777', 900: '#831843' } }
                }
            }
        }
    </script>
    <style>
        body { font-family: 'Quicksand', sans-serif; background-color: #fdf2f8; }
        .font-display { font-family: 'Fredoka', sans-serif; }
        .hide-scrollbar::-webkit-scrollbar { display: none; }
        .loader { border: 4px solid #fce7f3; border-top: 4px solid #ec4899; border-radius: 50%; width: 50px; height: 50px; animation: spin 1s linear infinite; margin: 20px auto; }
        @keyframes spin { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
    </style>
</head>
<body>
    
    <!-- CONTENEDOR DE ERRORES -->
    <div id="error-box" style="display:none; padding: 20px; background: #fee2e2; color: #991b1b; text-align: center; border-bottom: 2px solid #ef4444;">
        <h2 style="font-weight: bold;">⚠️ Error de Carga</h2>
        <p id="error-msg">Algo salió mal.</p>
    </div>

    <!-- CONTENEDOR APP -->
    <div id="root">
        <div style="min-height: 100vh; display: flex; flex-direction: column; align-items: center; justify-content: center;">
            <div class="loader"></div>
            <h2 class="text-2xl font-bold text-brand-600 font-display">Cargando Crystal Lotus...</h2>
        </div>
    </div>

    <!-- SCRIPT DE ERROR GLOBAL -->
    <script>
        window.onerror = function(message, source, lineno, colno, error) {
            document.getElementById('error-box').style.display = 'block';
            document.getElementById('error-msg').innerText = message + " (Línea: " + lineno + ")";
            document.getElementById('root').style.display = 'none';
        };
    </script>

    <!-- LÓGICA DE REACT -->
    <script type="text/babel">
        const { useState, useEffect } = React;

        // --- ICONOS SVG ---
        const Icons = {
            Bag: () => <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><path d="M6 2 3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4Z"/><path d="M3 6h18"/><path d="M16 10a4 4 0 0 1-8 0"/></svg>,
            User: () => <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><path d="M19 21v-2a4 4 0 0 0-4-4H9a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/></svg>,
            Lock: () => <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><rect width="18" height="11" x="3" y="11" rx="2" ry="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>,
            Unlock: () => <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><rect width="18" height="11" x="3" y="11" rx="2" ry="2"/><path d="M7 11V7a5 5 0 0 1 9.9-1"/></svg>,
            Search: () => <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><circle cx="11" cy="11" r="8"/><path d="m21 21-4.3-4.3"/></svg>,
            Plus: () => <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><path d="M5 12h14"/><path d="M12 5v14"/></svg>,
            X: () => <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><path d="M18 6 6 18"/><path d="m6 6 18 18"/></svg>,
            Trash: () => <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><path d="M3 6h18"/><path d="M19 6v14c0 1-1 2-2 2H7c-1 0-2-1-2-2V6"/><path d="M8 6V4c0-1 1-2 2-2h4c1 0 2 1 2 2v2"/><line x1="10" x2="10" y1="11" y2="17"/><line x1="14" x2="14" y1="11" y2="17"/></svg>,
            Edit: () => <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><path d="M17 3a2.828 2.828 0 1 1 4 4L7.5 20.5 2 22l1.5-5.5L17 3z"/></svg>,
            Share: () => <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><path d="M4 12v8a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2v-8"/><polyline points="16 6 12 2 8 6"/><line x1="12" x2="12" y1="2" y2="15"/></svg>,
            Star: () => <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="currentColor" stroke="none"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg>
        };

        // --- DATOS ---
        const InitialProducts = [
            { id: 1, name: "Cuaderno Tapa Blanda", price: 7000, description: "Personalizable, 48-80 hojas.", image: "https://images.unsplash.com/photo-1544816155-12df9643f363?w=500", category: "Cuadernos", inStock: true },
            { id: 2, name: "Cuaderno Tapa Rígida", price: 9000, description: "Super resistente y duradero.", image: "https://images.unsplash.com/photo-1531346878377-a51e195e8e56?w=500", category: "Cuadernos", inStock: true },
            { id: 3, name: "Anotador Rígido", price: 4000, description: "Ideal para llevar a todos lados.", image: "https://images.unsplash.com/photo-1517842645767-c639042777db?w=500", category: "Anotadores", inStock: true },
            { id: 4, name: "Anotador Blando", price: 2500, description: "Liviano y flexible.", image: "https://images.unsplash.com/photo-1595188882375-72c0506e78ba?w=500", category: "Anotadores", inStock: true },
            { id: 5, name: "Agenda 365 Días", price: 15000, description: "Planifica tu año completo.", image: "https://images.unsplash.com/photo-1506784365847-bbad939e9335?w=500", category: "Agendas", inStock: true },
            { id: 6, name: "Agenda Pediatras", price: 20000, description: "Organización profesional médica.", image: "https://images.unsplash.com/photo-1576091160399-112ba8d25d1d?w=500", category: "Agendas", inStock: true },
            { id: 7, name: "Agenda Manicuristas", price: 20000, description: "Turnos y clientas bajo control.", image: "https://images.unsplash.com/photo-1522337660859-02fbefca4702?w=500", category: "Agendas", inStock: true },
            { id: 8, name: "Photocards K-Pop", price: 800, description: "Doble faz, calidad premium.", image: "https://images.unsplash.com/photo-1612152843869-7d8719c8d002?w=500", category: "Fanmade", inStock: true },
            { id: 9, name: "Libro Colorear", price: 0, description: "20 ilustraciones relajantes.", image: "https://images.unsplash.com/photo-1513364776144-60967b0f800f?w=500", category: "Arte", inStock: true },
            { id: 10, name: "Pack Stickers", price: 2000, description: "Decorativos variados x10.", image: "https://images.unsplash.com/photo-1572375992501-4b0892d50c69?w=500", category: "Stickers", inStock: true },
            { id: 11, name: "Stickers Waterproof", price: 2500, description: "Resistentes al agua x5.", image: "https://images.unsplash.com/photo-1611566026373-c6c85447dbdc?w=500", category: "Stickers", inStock: true },
            { id: 12, name: "Fotos Alta Calidad", price: 1000, description: "Impresión nítida 10x14.", image: "https://images.unsplash.com/photo-1552168324-d612d77725e3?w=500", category: "Fotos", inStock: true },
            { id: 13, name: "Botella 750ml", price: 3500, description: "Plástico reutilizable.", image: "https://images.unsplash.com/photo-1602143407151-011141950038?w=500", category: "Accesorios", inStock: true }
        ];

        const Config = {
            name: "Crystal Lotus",
            sub: "Regalería",
            phone: "5491125573160",
            ig: "@crystal_lotus.nb"
        };

        const formatPrice = (p) => new Intl.NumberFormat('es-AR', { style: 'currency', currency: 'ARS', minimumFractionDigits: 0 }).format(p);

        // --- COMPONENTES ---

        const Hero = () => (
            <div className="relative h-64 md:h-80 w-full overflow-hidden mb-8 shadow-lg">
                <img src="https://images.unsplash.com/photo-1493934558415-9d19f0b2b4d2?w=1600" className="w-full h-full object-cover brightness-75" />
                <div className="absolute inset-0 flex flex-col items-center justify-center text-white text-center p-4">
                    <h1 className="text-4xl md:text-5xl font-display font-bold mb-2 drop-shadow-md">Crystal Lotus</h1>
                    <p className="text-lg md:text-xl font-light">Papelería con alma y diseño</p>
                </div>
            </div>
        );

        const ProductItem = ({ p, admin, onAdd, onEdit, onDelete, onPromote }) => (
            <div className="bg-white rounded-2xl shadow-sm hover:shadow-xl transition-all overflow-hidden border border-brand-100 flex flex-col h-full">
                <div className="relative h-48 bg-gray-100">
                    <img src={p.image} alt={p.name} className="w-full h-full object-cover" />
                    {p.inStock && <span className="absolute top-2 left-2 bg-white/90 text-brand-600 text-[10px] font-bold px-2 py-1 rounded-full shadow-sm">DISPONIBLE</span>}
                </div>
                <div className="p-4 flex flex-col flex-grow">
                    <span className="text-[10px] uppercase font-bold text-brand-400">{p.category}</span>
                    <h3 className="font-bold text-gray-800 text-lg leading-tight mb-1">{p.name}</h3>
                    <p className="text-gray-500 text-xs mb-3 flex-grow">{p.description}</p>
                    <div className="flex items-center justify-between mt-auto border-t border-gray-100 pt-3">
                        <span className="font-bold text-xl text-brand-600 font-display">{formatPrice(p.price)}</span>
                        {admin ? (
                            <div className="flex gap-1">
                                <button onClick={() => onPromote(p)} className="p-2 bg-blue-50 text-blue-500 rounded-lg"><Icons.Share/></button>
                                <button onClick={() => onEdit(p)} className="p-2 bg-yellow-50 text-yellow-600 rounded-lg"><Icons.Edit/></button>
                                <button onClick={() => onDelete(p.id)} className="p-2 bg-red-50 text-red-600 rounded-lg"><Icons.Trash/></button>
                            </div>
                        ) : (
                            <button onClick={() => onAdd(p)} disabled={!p.inStock} className={`px-3 py-2 rounded-xl font-bold text-xs text-white transition-colors ${p.inStock ? 'bg-brand-600 hover:bg-brand-900' : 'bg-gray-300'}`}>
                                {p.inStock ? 'AGREGAR' : 'AGOTADO'}
                            </button>
                        )}
                    </div>
                </div>
            </div>
        );

        const CartModal = ({ isOpen, onClose, cart, remove, total, checkout }) => {
            if (!isOpen) return null;
            return (
                <div className="fixed inset-0 z-50 flex justify-end bg-black/50 backdrop-blur-sm">
                    <div className="bg-white w-full max-w-md h-full flex flex-col shadow-2xl animate-fade-in">
                        <div className="p-4 border-b flex justify-between items-center bg-brand-50">
                            <h2 className="font-bold text-lg flex items-center gap-2"><Icons.Bag/> Tu Carrito</h2>
                            <button onClick={onClose}><Icons.X/></button>
                        </div>
                        <div className="flex-1 overflow-y-auto p-4 space-y-3">
                            {cart.length === 0 ? <p className="text-center text-gray-400 mt-10">Nada por aquí...</p> : 
                                cart.map((item, i) => (
                                    <div key={i} className="flex gap-3 border p-2 rounded-xl">
                                        <img src={item.image} className="w-12 h-12 rounded-lg object-cover" />
                                        <div className="flex-1">
                                            <p className="font-bold text-sm">{item.name}</p>
                                            <p className="text-brand-600 text-xs font-bold">{formatPrice(item.price)}</p>
                                        </div>
                                        <button onClick={() => remove(i)} className="text-red-400"><Icons.Trash/></button>
                                    </div>
                                ))
                            }
                        </div>
                        <div className="p-4 border-t bg-gray-50">
                            <div className="flex justify-between font-bold text-xl mb-4"><span>Total:</span><span>{formatPrice(total)}</span></div>
                            <button onClick={checkout} disabled={cart.length===0} className="w-full bg-brand-600 text-white py-3 rounded-xl font-bold">Pedir por WhatsApp</button>
                        </div>
                    </div>
                </div>
            );
        };

        const AuthModal = ({ isOpen, onClose, onLogin, usersDb }) => {
            const [isLogin, setIsLogin] = useState(true);
            const [form, setForm] = useState({ name: '', email: '', password: '' });
            
            if (!isOpen) return null;

            const submit = () => {
                if (!form.email || !form.password) return alert("Faltan datos");
                if (isLogin) {
                    const u = usersDb.find(u => u.email === form.email && u.password === form.password);
                    if (u) onLogin(u); else alert("Datos incorrectos");
                } else {
                    if (!form.name) return alert("Nombre requerido");
                    onLogin({ ...form, history: [] });
                }
            };

            return (
                <div className="fixed inset-0 z-50 flex items-center justify-center p-4 bg-black/60 backdrop-blur-sm">
                    <div className="bg-white rounded-2xl w-full max-w-sm p-6 relative">
                        <button onClick={onClose} className="absolute top-3 right-3 text-gray-400"><Icons.X/></button>
                        <h2 className="text-xl font-bold text-center mb-4">{isLogin ? 'Ingresar' : 'Registrarse'}</h2>
                        <div className="space-y-3">
                            {!isLogin && <input placeholder="Nombre" className="w-full border p-2 rounded-lg" value={form.name} onChange={e=>setForm({...form,name:e.target.value})} />}
                            <input placeholder="Email" className="w-full border p-2 rounded-lg" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} />
                            <input type="password" placeholder="Contraseña" className="w-full border p-2 rounded-lg" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} />
                            <button onClick={submit} className="w-full bg-brand-600 text-white py-2 rounded-lg font-bold">Enviar</button>
                            <p className="text-center text-xs text-brand-500 cursor-pointer" onClick={()=>setIsLogin(!isLogin)}>{isLogin ? 'Crear cuenta' : 'Ya tengo cuenta'}</p>
                        </div>
                    </div>
                </div>
            );
        };

        const PromoModal = ({ isOpen, onClose, p, cfg }) => {
            if (!isOpen || !p) return null;
            const txt = `✨ ${p.name}\n💰 ${formatPrice(p.price)}\n${p.description}\n👉 ${window.location.href}`;
            return (
                <div className="fixed inset-0 z-50 flex items-center justify-center p-4 bg-black/60 backdrop-blur-sm">
                    <div className="bg-white rounded-2xl p-6 w-full max-w-sm relative text-center">
                        <button onClick={onClose} className="absolute top-3 right-3"><Icons.X/></button>
                        <h3 className="font-bold text-lg mb-2 text-brand-600">¡Promociona esto!</h3>
                        <img src={p.image} className="w-32 h-32 mx-auto rounded-lg mb-4 object-cover shadow-md" />
                        <textarea readOnly className="w-full border rounded p-2 text-xs h-24 mb-3" value={txt}></textarea>
                        <button onClick={()=>{navigator.clipboard.writeText(txt); alert("Copiado")}} className="bg-gray-900 text-white px-4 py-2 rounded-lg text-sm font-bold w-full">Copiar Texto</button>
                    </div>
                </div>
            );
        };

        const App = () => {
            const [products, setProducts] = useState(InitialProducts);
            const [admin, setAdmin] = useState(false);
            const [cart, setCart] = useState([]);
            const [isCartOpen, setIsCartOpen] = useState(false);
            const [user, setUser] = useState(null);
            const [usersDb, setUsersDb] = useState([]);
            const [isAuthOpen, setIsAuthOpen] = useState(false);
            const [search, setSearch] = useState("");
            const [promoP, setPromoP] = useState(null);

            // Cargar datos
            useEffect(() => {
                const ls = (k) => localStorage.getItem(k);
                if (ls('cl_p_v11')) setProducts(JSON.parse(ls('cl_p_v11')));
                if (ls('cl_u_db')) setUsersDb(JSON.parse(ls('cl_u_db')));
                if (ls('cl_curr')) setUser(JSON.parse(ls('cl_curr')));
            }, []);

            // Guardar datos
            useEffect(() => {
                localStorage.setItem('cl_p_v11', JSON.stringify(products));
                localStorage.setItem('cl_u_db', JSON.stringify(usersDb));
                user ? localStorage.setItem('cl_curr', JSON.stringify(user)) : localStorage.removeItem('cl_curr');
            }, [products, usersDb, user]);

            const toggleAdmin = () => {
                if (admin) setAdmin(false);
                else {
                    const p = prompt("PIN:");
                    if (p === "5656") setAdmin(true); else alert("Error");
                }
            };

            const checkout = () => {
                const total = cart.reduce((s,i)=>s+i.price,0);
                let msg = `Hola *${Config.name}*!\nPedido:\n`;
                cart.forEach(i => msg += `- ${i.name}\n`);
                msg += `\nTotal: ${formatPrice(total)}`;
                if(user) {
                    const newU = {...user, history: [...(user.history||[]), {date: new Date(), total, items: cart}]};
                    setUser(newU);
                    setUsersDb(prev => {
                        const idx = prev.findIndex(u => u.email === user.email);
                        if(idx >= 0) prev[idx] = newU; else prev.push(newU);
                        return [...prev];
                    });
                    msg += `\nCliente: ${user.name}`;
                }
                msg += `\nEnvío: Correo Argentino`;
                window.open(`https://wa.me/${Config.phone}?text=${encodeURIComponent(msg)}`, '_blank');
                setCart([]); setIsCartOpen(false);
            };

            const handleLogin = (u) => {
                const exists = usersDb.find(ex => ex.email === u.email);
                if (!exists) setUsersDb([...usersDb, u]);
                setUser(u); setIsAuthOpen(false);
            };

            return (
                <div className="pb-20">
                    {/* Header */}
                    <div className="bg-brand-600 text-white text-[10px] font-bold text-center py-2 tracking-widest">
                        🚚 ENVÍOS A TODO EL PAÍS POR CORREO ARGENTINO
                    </div>
                    <nav className="sticky top-0 bg-white/90 backdrop-blur-md border-b border-brand-100 z-40 px-4 py-3 flex justify-between items-center shadow-sm">
                        <div className="flex items-center gap-2">
                            <div className="bg-brand-100 p-2 rounded-lg text-brand-600 font-bold font-display text-xl">CL</div>
                            <div>
                                <h1 className="font-display font-bold text-gray-800 leading-none">{Config.name}</h1>
                                <p className="text-[10px] font-bold text-brand-500 uppercase tracking-widest">{Config.sub}</p>
                            </div>
                        </div>
                        <div className="flex gap-3">
                            {!admin && <button onClick={()=>setIsCartOpen(true)} className="relative p-2 text-gray-600"><Icons.Bag/><span className="absolute top-0 right-0 bg-brand-500 text-white text-[10px] w-4 h-4 rounded-full flex items-center justify-center">{cart.length}</span></button>}
                            {!admin && <button onClick={()=>setIsAuthOpen(true)} className="p-2 text-gray-600"><Icons.User/></button>}
                            <button onClick={toggleAdmin} className={`p-2 rounded-full ${admin?'bg-brand-500 text-white':'text-gray-400'}`}>{admin?<Icons.Unlock/>:<Icons.Lock/>}</button>
                        </div>
                    </nav>

                    <Hero />

                    {/* Main */}
                    <div className="max-w-6xl mx-auto px-4">
                        <div className="mb-8 relative">
                            <span className="absolute left-3 top-3 text-gray-400"><Icons.Search/></span>
                            <input className="w-full pl-10 pr-4 py-3 rounded-xl border-none shadow-sm focus:ring-2 focus:ring-brand-200" placeholder="¿Qué buscas hoy?" value={search} onChange={e=>setSearch(e.target.value)} />
                        </div>

                        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
                            {products.filter(p => p.name.toLowerCase().includes(search.toLowerCase())).map(p => (
                                <ProductItem 
                                    key={p.id} 
                                    p={p} 
                                    admin={admin} 
                                    onAdd={()=>setCart([...cart, p])} 
                                    onDelete={(id)=>confirm("¿Borrar?") && setProducts(products.filter(pr=>pr.id!==id))}
                                    onEdit={()=>{/* Implementar edición simple si se desea */}}
                                    onPromote={(prod)=>{setPromoP(prod)}}
                                />
                            ))}
                        </div>
                        
                        {/* Testimonios */}
                        <div className="mt-16 bg-white p-8 rounded-3xl shadow-sm text-center">
                            <h2 className="text-2xl font-display font-bold text-gray-800 mb-6">Clientes Felices 💖</h2>
                            <div className="grid md:grid-cols-3 gap-6">
                                <div className="bg-brand-50 p-4 rounded-xl"><div className="flex justify-center text-yellow-400 mb-2"><Icons.Star/><Icons.Star/><Icons.Star/><Icons.Star/><Icons.Star/></div><p className="italic text-sm text-gray-600">"Hermoso todo, llegó rápido."</p><p className="font-bold text-xs mt-2">- Sofi</p></div>
                                <div className="bg-brand-50 p-4 rounded-xl"><div className="flex justify-center text-yellow-400 mb-2"><Icons.Star/><Icons.Star/><Icons.Star/><Icons.Star/><Icons.Star/></div><p className="italic text-sm text-gray-600">"Los cuadernos son increíbles."</p><p className="font-bold text-xs mt-2">- Cami</p></div>
                                <div className="bg-brand-50 p-4 rounded-xl"><div className="flex justify-center text-yellow-400 mb-2"><Icons.Star/><Icons.Star/><Icons.Star/><Icons.Star/><Icons.Star/></div><p className="italic text-sm text-gray-600">"Súper recomendado."</p><p className="font-bold text-xs mt-2">- Valen</p></div>
                            </div>
                        </div>
                    </div>

                    {/* Footer */}
                    <footer className="mt-12 bg-gray-900 text-white py-10 px-4 text-center">
                        <h2 className="font-display font-bold text-xl mb-4">{Config.name}</h2>
                        <p className="text-gray-400 text-sm mb-6">Tienda online con amor.</p>
                        <div className="text-xs text-gray-500">© 2024 Todos los derechos reservados.</div>
                    </footer>

                    {/* Modales */}
                    <CartModal isOpen={isCartOpen} onClose={()=>setIsCartOpen(false)} cart={cart} remove={(i)=>{const c=[...cart];c.splice(i,1);setCart(c)}} total={cart.reduce((s,i)=>s+i.price,0)} checkout={checkout}/>
                    <AuthModal isOpen={isAuthOpen} onClose={()=>setIsAuthOpen(false)} onLogin={handleLogin} usersDb={usersDb} />
                    <PromoModal isOpen={!!promoP} onClose={()=>setPromoP(null)} p={promoP} cfg={Config} />
                </div>
            );
        };

        const root = ReactDOM.createRoot(document.getElementById('root'));
        root.render(<App />);
    </script>
</body>
</html>


